import { productRating } from 'online-marketplace/helpers/product-rating';
import { module, test } from 'qunit';

module('Unit | Helper | product rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productRating([42]);
  assert.ok(result);
});
